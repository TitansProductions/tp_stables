# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added fleeing, relog and server restart support for placed horse pelts to stay and load when whistling the horse (optional in config)

## Modified Files:

- client/horses.lua
- server/main.lua
- config.lua (check notes
- fxmanifest.lua

## Notes

- Add on the `config.lua` file below `Config.AllowStowSmallPeltOnHorse`:

```lua

-- Set to true if you want the stowed carriable pelts (placed small pelts) to load after fleeing a horse or server restarts.
Config.LoadStowedSmallPeltsOnHorseFleeOrLogout = true -- 1.0.6
```
